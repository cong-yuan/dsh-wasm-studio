{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window and plugin windows",
  "windows": [
    "main",
    "plugin-*"
  ],
  "permissions": [
    "core:default",
    "opener:default",
    "dialog:default"
  ]
}
